---
# defaults
#

docker_users: ["ansible"]
harbor_version: "v2.0.2"

unwanted_docker_packages:
    - docker 
    - docker-client 
    - docker-client-latest 
    - docker-common 
    - docker-latest 
    - docker-latest-logrotate 
    - docker-logrotate 
    - docker-engine

docker_packages:
    - docker-ce 
    - docker-ce-cli 
    - containerd.io


docker_compose_version: "2.6.0-3"